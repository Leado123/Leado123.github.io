export interface Class {
    id: number;
    className: string;
    fullClassName: string;
    disciplineId: string;
}

export interface School {
    id: string;
    name: string;
    fullName: string;
}

export interface Professor {
    id: string;
    name: string;
}